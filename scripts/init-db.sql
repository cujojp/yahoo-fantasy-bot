-- Initialize database for Yahoo Fantasy Bot local development
-- The application uses Exposed ORM which will create tables automatically
-- This script just ensures the database is ready

SELECT 'Database initialized for Yahoo Fantasy Bot' as status;
